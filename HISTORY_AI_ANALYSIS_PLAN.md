# แผน Phase 2: วิเคราะห์ประวัติแชตด้วย AI

## สถานะก่อนเริ่ม

- Phase 1 เสร็จแล้ว: ข้อมูล Facebook ถูกเก็บแยกไว้ในฐานข้อมูลในเครื่อง
  (`history_conversations`, `history_messages`)
- ข้อมูลที่เก็บ: 608 บทสนทนา และ 10,745 ข้อความตัวอักษร
- ช่วงเวลา: 16 มิ.ย. – 11 ส.ค. 2026 (ย้อนหลังไม่เกิน 60 วัน)
- ไม่มีรูปภาพ/สื่อ และไม่มีการส่งข้อความกลับลูกค้า
- Phase 2 จะ **ไม่แก้ flow ระบบหลัก** และ **ไม่ส่งข้อความ Facebook**

## เป้าหมาย

ใช้ AI วิเคราะห์ข้อมูลที่อยู่ในเครื่อง เพื่อสร้างชุดข้อมูลตั้งค่าสำหรับ
1. รับออเดอร์ด้วยกฎ (ไม่เรียก AI ระหว่างคุยกับลูกค้า)
2. ตอบเฉพาะคำถามที่มีข้อมูลยืนยัน
3. ส่งต่อแอดมินเมื่อไม่ควรเดา
4. นำโครงสร้างเดียวกันไปใช้กับร้านประเภทอื่นได้

## ขั้นตอนที่จะทำ

### 1. เตรียมชุดข้อมูลส่งวิเคราะห์และชุดติดป้ายกำกับ

- อ่านเฉพาะข้อความจากตาราง `history_*` ในเครื่อง
- อ่านข้อมูลสินค้าและตัวเลือกที่ยืนยันแล้วจากตารางตั้งค่าร้านในเครื่อง
  (`products`, product modifiers และ ingredients) เพื่อใช้เป็น catalog อ้างอิงเท่านั้น
  โดยไม่ดึงข้อมูลลูกค้าหรือข้อมูล live Inbox
- ตัด/ปกปิดข้อมูลระบุตัวบุคคลที่ไม่จำเป็น (เช่น ชื่อ, เบอร์, เลขบ้าน/ห้อง,
  รหัสไปรษณีย์, พิกัดและลิงก์แผนที่, รหัส Facebook)
- คงได้เฉพาะบริบทจุดส่งแบบคร่าว ๆ ที่ไม่เจาะจงบุคคล เช่น ประเภทสถานที่หรือโซน
  (`หอในโซน A`, `หน้าร้าน`) เพื่อวิเคราะห์ flow จัดส่ง
- แยกเป็นชุดย่อยที่มีบริบทพอเหมาะต่อการวิเคราะห์
- เก็บสำเนาชุดที่เตรียมและผลวิเคราะห์ไว้ในเครื่อง
- ก่อนการเรียก AI จริง ให้แสดงตัวอย่างและขออนุมัติจากผู้ดูแลก่อนเสมอ
- ผลจากการวิเคราะห์แต่ละข้อความจะถูกเก็บเป็นข้อมูลติดป้ายกำกับแบบ `draft`
  เพื่อใช้สร้างและทดสอบกฎ ไม่ใช้เป็นคำตอบลูกค้าโดยตรง

### 2. วิเคราะห์รูปแบบการสั่งสินค้า

AI สรุปจากข้อความจริงเป็นข้อมูลเชิงโครงสร้าง เช่น

- ชื่อสินค้า, ชื่อเรียกอื่น, คำย่อ, การสะกดผิดที่พบบ่อย
- จำนวน, หน่วยนับ, รายการหลายรายการในข้อความเดียว
- ตัวเลือกสินค้า เช่น ปกติ/พิเศษ
- คำสั่งแก้ไข เพิ่ม ลด ยกเลิก หรือสั่งซ้ำ
- ข้อความกำกวมที่ต้องถามกลับ

ผลลัพธ์: `order_patterns` และ `product_aliases` สำหรับให้ตัวรับออเดอร์แบบ rule-based ใช้ภายหลัง

### 3. สร้างคลังเจตนาและคำตอบ

AI จัดกลุ่มข้อความเป็นเจตนา พร้อมระดับความมั่นใจและแหล่งอ้างอิงจากประวัติ เช่น

- คำทักทาย / ขอเมนู / ดูราคา
- เริ่มสั่ง / เพิ่มรายการ / แก้ไข / ยกเลิก
- ถามค่าจัดส่ง (ตอบได้: ฟรีค่าจัดส่ง)
- ถามเรื่องที่ควรให้แอดมินตอบ
- ข้อความไม่ชัดเจนหรือไม่เกี่ยวกับการสั่ง

ผลลัพธ์: `intent_rules`, `faq_answerable`, `admin_handoff_rules`

### 4. แยก "ตอบได้" ออกจาก "ต้องรอแอดมิน"

| กรณี | การทำงานที่ต้องการ |
| --- | --- |
| เมนู / ราคา / ค่าส่ง | ตอบอัตโนมัติได้เมื่อข้อมูลตั้งค่าไว้ |
| เวลาเปิด–ปิด | ไม่เดา, ติด `รอแอดมินตอบ` |
| คิวนานไหม / ได้อาหารเมื่อไร | ไม่เดา, ติด `รอแอดมินตอบ` |
| ของหมด / สต็อก / เงื่อนไขพิเศษ | ติด `รอแอดมินตอบ` หากไม่มีสถานะยืนยัน |
| คำถามใหม่หรือกำกวม | ติด `รอแอดมินตอบ` |
| ต่อรองราคา / ร้องเรียน / ปัญหาออเดอร์ | ติด `รอแอดมินตอบ` |

หลักการ: ถ้าไม่มีข้อมูลยืนยัน ระบบต้องไม่สร้างคำตอบขึ้นเอง

### 5. สร้างคำถามที่ระบบต้องถามกลับเพื่อปิดออเดอร์

นี่ไม่ใช่ FAQ แต่เป็น checklist ที่ใช้เมื่อข้อมูลออเดอร์ยังไม่ครบ เช่น

1. มีสินค้าและจำนวนครบหรือไม่
2. ตัวเลือกที่จำเป็นครบหรือไม่ (เช่น ปกติ/พิเศษ)
3. สำหรับเดลิเวอรี่: ขอที่อยู่, พิกัดหรือจุดสังเกตตามความจำเป็น
4. ถ้ามีข้อมูลขัดแย้งหรือไม่ชัด: ถามยืนยันเฉพาะจุดนั้น

ผลลัพธ์: `order_completion_questions` และลำดับการถามกลับแบบกำหนดกฎ

### 6. ค้นหากรณีนอกเหนือจากข้อมูลจริง

AI จะเสนอกรณีที่น่าจะพบในร้านค้า ไม่จำกัดเฉพาะร้านอาหาร เช่น

- สินค้าหมด, จำนวนไม่พอ, เปลี่ยนรายการ, ยกเลิก
- ลูกค้าส่งข้อความหลายเรื่องพร้อมกัน
- ข้อมูลจัดส่งไม่ครบหรือไม่ชัดเจน
- คำถามนโยบายร้าน, ข้อร้องเรียน, ขอคุยกับคน
- ข้อความพิมพ์ผิด, ภาษาพูด, อีโมจิ, ข้อความสั้นมาก

ข้อเสนอทุกข้อจะติดสถานะ `ต้องตรวจทาน` จนกว่าผู้ดูแลจะอนุมัติ ไม่มีการนำไปตอบจริงเอง

### 7. สร้างข้อมูลตั้งค่าแบบใช้ซ้ำได้หลายร้าน

แยกข้อมูลเฉพาะร้านออกจากกลไกกลาง:

- `store_profile`: ประเภทร้าน, นโยบาย, ช่องทางจัดส่ง
- `products`: สินค้า, ราคา, ตัวเลือก, ชื่อเรียกอื่น
- `answerable_faq`: คำตอบที่ยืนยันแล้ว
- `admin_handoff_rules`: สิ่งที่ห้ามเดา
- `order_completion_questions`: สิ่งที่ต้องถามก่อนยืนยัน
- `intent_rules` / `order_patterns`: กฎตีความข้อความ

จึงนำไปตั้งค่าร้านกาแฟ เสื้อผ้า หรือสินค้าอื่นได้โดยไม่ต้องเปลี่ยนกลไกหลัก

## รูปแบบการส่งข้อมูลให้ AI (เมื่ออนุมัติ)

1. ระบบสร้างชุดข้อความที่ปกปิดข้อมูลส่วนบุคคลแล้วไว้ในเครื่อง
2. ผู้ดูแลตรวจตัวอย่างและกดอนุมัติเป็นครั้ง ๆ
3. ส่งเฉพาะชุดที่อนุมัติไปยัง AI เพื่อวิเคราะห์
4. เก็บผลลัพธ์ในเครื่อง พร้อมสถานะ `draft`
5. ผู้ดูแลตรวจ/แก้ไข/อนุมัติผลลัพธ์
6. เฉพาะข้อมูลที่อนุมัติเท่านั้นจึงค่อยถูกนำไปสร้างกฎในระบบหลักภายหลัง

## ข้อห้ามตลอด Phase 2

- ห้ามส่งข้อความ Facebook หรือเรียก API ส่งข้อความ
- ห้ามให้ AI ตอบลูกค้าโดยตรง
- ห้ามแก้ข้อมูลหรือ flow ของระบบหลักอัตโนมัติ
- ห้ามใช้ข้อมูลส่วนบุคคลเกินความจำเป็นในการวิเคราะห์
- ห้ามนำคำตอบที่ AI คาดเดาไปใช้งานจริงโดยไม่ผ่านการอนุมัติ

## 8. ต่อยอดตัวรับออเดอร์แบบ Rule-based (ไม่เรียก AI ระหว่างคุย)

ใช้ข้อมูลและกฎที่ผ่านการตรวจทานจากขั้นตอนก่อนหน้า เพื่อพัฒนา parser ที่ทำงาน
ในเครื่องและอธิบายเหตุผลของผลลัพธ์ได้ โดยไม่เปลี่ยน flow ระบบหลักอัตโนมัติ

### 8.1 Text normalization และตัดคำภาษาไทย

- แปลงตัวเลขไทยเป็นตัวเลขอารบิก (`๑` → `1`) และปรับรูปแบบ `x2`, `2x`, `2 จาน`
- ลดช่องว่าง/สัญลักษณ์ที่ไม่จำเป็น โดยคงข้อความเดิมไว้สำหรับแสดงผล
- ใช้ PyThaiNLP (`newmm`) ร่วมกับพจนานุกรมชื่อสินค้าและคำเรียกของร้าน เพื่อช่วยแบ่งคำไทย
- เก็บผลการ normalize แยกจากข้อความต้นฉบับเสมอ

### 8.2 คลังสินค้า คำเรียกอื่น และคำย่อ

- เพิ่ม `product_aliases`: ชื่อเรียกอื่น, คำสะกดที่พบจริง, และคำย่อที่ผู้ดูแลอนุมัติ
- จับคู่ชื่อสินค้าแบบตรงตัวและ alias ก่อนเสมอ
- ห้ามสร้าง alias จากการเดา: ข้อเสนอจากประวัติหรือ AI ต้องมีสถานะ `draft` จนกว่าจะอนุมัติ

### 8.3 Regex และการดึงข้อมูลออเดอร์

- สร้าง pattern แบบกำหนดกฎสำหรับเจตนาหลัก เช่น เริ่มสั่ง, เพิ่ม/ลด/ยกเลิก, ถามราคา และขอเมนู
- ดึง slot ที่จำเป็น ได้แก่ สินค้า, จำนวน, หน่วยนับ, ตัวเลือก และคำขอพิเศษ
- รองรับรูปแบบจำนวนที่อยู่หน้าหรือหลังสินค้า เช่น `เอา 2 กะเพรา`, `กะเพรา 2 จาน`
- ผลการจับคู่ทุกครั้งต้องระบุแหล่งที่มา (`exact`, `alias`, `regex`) และระดับความมั่นใจ

### 8.4 Fuzzy matching สำหรับคำพิมพ์ผิด

- ใช้ Levenshtein distance หรือ string similarity เฉพาะเมื่อไม่พบชื่อสินค้า/alias แบบตรงตัว
- รับผลได้เมื่อคะแนนถึงเกณฑ์ที่กำหนด และสูงกว่าตัวเลือกอันดับสองมากพอ
- หากมีความกำกวมหรือคะแนนไม่ถึงเกณฑ์ ให้ถามยืนยันหรือส่งแอดมิน ห้ามเลือกสินค้าเอง
- ผลจาก fuzzy matching ต้องติดป้ายว่าเป็นการคาดเดา เพื่อให้ตรวจทานได้

### 8.5 Intent classification แบบอธิบายได้

- เริ่มจาก keyword weighting และกฎที่ผู้ดูแลแก้ไขได้ เช่น `ราคา`, `กี่บาท` → `ask_price`
- บันทึก intent ที่เข้าข่าย, คีย์เวิร์ดที่พบ และคะแนนของแต่ละ intent
- พิจารณา Naive Bayes หรือโมเดลสถิติเบา ๆ ภายหลัง เมื่อมีข้อความที่ผู้ดูแลติดป้าย intent แล้วเพียงพอ
- ไม่ใช้ชุดข้อมูลสาธารณะเป็นแหล่งคำตอบหรือข้อมูลร้าน; ใช้ได้เพียงเป็นข้อมูลประกอบการประเมินโมเดล

### 8.6 State machine และคำถามเพื่อปิดออเดอร์

- กำหนดสถานะ เช่น `waiting_for_items` → `waiting_for_required_options` → `waiting_for_delivery_details` → `waiting_for_confirmation`
- เก็บบริบทเฉพาะที่จำเป็นต่อออเดอร์ปัจจุบัน และไม่ตีความข้อความเก่าซ้ำโดยอัตโนมัติ
- ถ้าข้อมูลขัดแย้ง, ไม่ครบ, หรือคำถามอยู่นอกกฎ ให้เปลี่ยนเป็น `waiting_for_admin`
- การยืนยันออเดอร์ต้องเกิดหลังได้สินค้า/จำนวน/ตัวเลือก/ข้อมูลจัดส่งครบตามประเภทคำสั่งซื้อเท่านั้น
- เมื่อข้อความมีคำถาม ให้ตอบหรือส่งแอดมินเพื่อปิดคำถามก่อน; ห้ามเริ่ม/เพิ่มออเดอร์จากการพบชื่อสินค้า
  ในข้อความคำถามจนกว่าลูกค้าจะกลับมายืนยันการสั่ง

### 8.7 การเก็บ feedback เพื่อปรับกฎ

- เพิ่ม log สำหรับคำหรือวลีที่ไม่จับคู่สินค้า (`unmatched_terms`) และผล fuzzy ที่ถูกปฏิเสธ
- ให้ผู้ดูแลเปลี่ยน log เหล่านี้เป็น alias, rule หรือ admin handoff rule ได้หลังตรวจทาน
- ทดสอบทุกกฎด้วยข้อความที่ปกปิดข้อมูลส่วนบุคคล และมี regression test ก่อนเปิดใช้
- ทุกกรณี `waiting_for_admin` ต้องเก็บประวัติในเครื่อง: ข้อความที่ปกปิดแล้ว, intent/candidate,
  เหตุผลส่งต่อ, สถานะ, คำตัดสิน/คำตอบของแอดมิน และเวลาปิดเคส เพื่อใช้สร้าง rule หรือเพิ่ม catalog ต่อไป

### ลำดับการทำ

1. Normalization, ตัวเลข/หน่วยนับ และ `unmatched_terms`
2. `product_aliases` และ exact/alias matching
3. Regex extraction สำหรับสินค้า/จำนวน/ตัวเลือก และ intent rules แบบ keyword weighting
4. State machine และคำถามปิดออเดอร์
5. Fuzzy matching พร้อม threshold, ความต่างจากอันดับสอง และการถามยืนยัน
6. Naive Bayes หรือโมเดลสถิติ (ทำเมื่อมีข้อมูลที่ติดป้ายกำกับจากผู้ดูแลเพียงพอ)

## 9. ชุดข้อมูลที่ต้องสร้างระหว่างวิเคราะห์ประวัติแชต

Phase 2 ต้องสร้างข้อมูลทั้ง 6 ส่วนไปพร้อมกับการวิเคราะห์ ไม่ใช่เพียงสรุปข้อความ
โดยให้ทุกข้อเสนอเริ่มต้นเป็น `draft` และเปลี่ยนเป็น `approved` ได้เฉพาะเมื่อผู้ดูแลตรวจแล้ว

### 9.1 แหล่งข้อมูลในเครื่อง

| แหล่งข้อมูล | ใช้สร้างอะไร | ข้อจำกัด |
| --- | --- | --- |
| `history_messages` | ข้อความตัวอย่าง, intent, รูปแบบการสั่ง, คำสะกดผิด, คำถาม, ข้อความกำกวม | ปกปิด PII ก่อนใช้ทุกครั้ง |
| `history_conversations` | ลำดับบริบทในบทสนทนาและการแก้/เพิ่ม/ยกเลิกรายการ | ไม่ส่งชื่อหรือรหัส Facebook ออกไป |
| `products` และ product modifiers | ชื่อมาตรฐาน, ราคา, ตัวเลือกที่ถูกต้อง, catalog สำหรับเทียบคำในแชต | เป็นข้อมูลอ้างอิงที่อนุมัติแล้วเท่านั้น |
| `ingredients` (ถ้าใช้เป็นตัวเลือกในคำสั่งซื้อ) | คำขอพิเศษ เช่น ไม่ใส่/เพิ่มวัตถุดิบ | ไม่ใช้สรุปสถานะสต็อกเพื่อตอบลูกค้า |
| การแก้ไขของผู้ดูแล | alias ที่ถูกต้อง, intent จริง, threshold, คำถามตอบกลับ และเหตุผลส่งแอดมิน | เป็นแหล่งความจริงสูงสุด |

ชุดข้อมูล open-source ภายนอกใช้ได้เฉพาะเป็นตัวอย่าง schema หรือเพื่อประเมินแนวทาง
เช่น tokenization/intent/slot filling เท่านั้น ห้ามนำข้อความภายนอกมาเป็นข้อเท็จจริงของร้าน
หรือใช้สร้างคำตอบ/เมนู/ราคาโดยอัตโนมัติ

### 9.2 รูปแบบข้อมูลติดป้ายกำกับต่อข้อความ

สำหรับข้อความที่ปกปิดข้อมูลแล้วแต่ละรายการ ให้เก็บข้อมูลอย่างน้อยดังนี้:

```json
{
  "sample_id": "local-only",
  "redacted_text": "เอากะเพา 2 จาน",
  "normalized_text": "เอากะเพรา 2 จาน",
  "tokens": ["เอา", "กะเพรา", "2", "จาน"],
  "intent": "start_order",
  "entities": [
    {"type": "product", "canonical_product_id": 12, "observed_text": "กะเพา", "match": "alias"},
    {"type": "quantity", "value": 2},
    {"type": "unit", "value": "จาน"}
  ],
  "next_action": "collect_required_options",
  "handoff_reason": null,
  "review_status": "draft"
}
```

ห้ามเก็บชื่อ, เบอร์โทร, เลขบ้าน/ห้อง, รหัสไปรษณีย์, พิกัด, Facebook ID,
URL เฉพาะบุคคล หรือ payload ดิบในชุดติดป้ายกำกับนี้ โดยเก็บได้เฉพาะบริบทจุดส่งคร่าว ๆ

### 9.3 ผลลัพธ์ที่ต้องได้สำหรับทั้ง 6 ส่วน

| ส่วน | ข้อมูลที่สกัดจากประวัติ | ผลลัพธ์ที่จัดเก็บเป็นร่าง |
| --- | --- | --- |
| Text normalization | รูปเลขไทย/อารบิก, เว้นวรรค, เครื่องหมาย, คำย่อที่พบ | `normalization_rules`, ตัวอย่างก่อน/หลัง normalize |
| Dictionary + aliases | คำสะกด, ชื่อเล่น, คำย่อที่ถูกจับคู่กับสินค้าจริง | `product_aliases` พร้อมสินค้าอ้างอิง, ความถี่, ตัวอย่างข้อความ |
| Regex extraction | คำกริยาเริ่มสั่ง, รูปแบบจำนวน/หน่วย, ราคา, จัดส่ง/ชำระเงิน | `order_patterns`, `intent_rules` พร้อมตัวอย่างที่ match และไม่ match |
| Fuzzy matching | คำที่ไม่ match, ผู้สมัครสินค้าที่ใกล้เคียง, ผลที่ผู้ดูแลยอมรับ/ปฏิเสธ | `fuzzy_match_reviews`, threshold และ margin ที่ผ่านการอนุมัติ |
| Intent rules / Naive Bayes | ข้อความที่ติดป้าย intent โดยผู้ดูแล, คีย์เวิร์ด และคะแนน | `intent_training_examples`, `intent_keyword_weights`; โมเดลสถิติเป็น artifact แยกและต้องทดสอบ |
| State machine | ลำดับข้อความ, ข้อมูลที่มักขาด, การถามกลับ, การแก้/ยกเลิก และการส่งแอดมิน | `order_completion_questions`, `state_transitions`, `admin_handoff_rules` |

### 9.4 Workflow ระหว่างวิเคราะห์

1. สร้าง batch ข้อความที่ปกปิด PII และ catalog สินค้าที่เกี่ยวข้องในเครื่อง
2. ผู้ดูแลตรวจตัวอย่างและอนุมัติ batch ก่อนส่งไปวิเคราะห์กับ AI (หากเลือกใช้ AI)
3. AI หรือเครื่องมือ rule-based เสนอ label, alias, pattern, fuzzy candidate, intent และ transition ในสถานะ `draft`
4. ผู้ดูแลแก้ไข/อนุมัติ/ปฏิเสธแต่ละข้อเสนอ พร้อมบันทึกเหตุผลที่จำเป็น
5. นำเฉพาะข้อมูล `approved` ไปสร้าง rule configuration และ regression test ใน environment ทดสอบ
6. วัดผลจากข้อความจริงที่ผ่านการปกปิดและกันไว้เป็น test corpus; ถ้ากำกวม, คะแนนต่ำ หรือไม่มี rule ที่อนุมัติ ให้ผลเป็น `waiting_for_admin`
7. การนำกฎไปเชื่อมกับ flow รับออเดอร์จริงเป็นงานระยะถัดไป และต้องอนุมัติแยกต่างหาก

## สิ่งที่ต้องตัดสินใจก่อนลงมือ Phase 2

1. ผู้ให้บริการ/บัญชี AI ที่จะใช้ และงบหรือโควตาที่อนุญาต
2. ระดับการปกปิดข้อมูล: ปิดบังเฉพาะชื่อ/เบอร์/ที่อยู่ หรือปิดบังข้อมูลอื่นเพิ่ม
3. รูปแบบหน้าตรวจทานผลลัพธ์: ไฟล์ก่อน หรือหน้าแอดมินในระบบ
4. ผู้มีสิทธิ์อนุมัติชุดข้อมูลและกฎก่อนนำไปใช้
