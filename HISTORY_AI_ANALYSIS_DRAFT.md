# ผลวิเคราะห์ Phase 2 — ร่างสำหรับตรวจทาน

สถานะ: `parser_v2_test_ready`  
แหล่งข้อมูล: snapshot #2 ที่อนุมัติแล้ว 63 ชุดย่อย, 608 บทสนทนา, 10,745 ข้อความ  
ขอบเขต: วิเคราะห์เฉพาะข้อความที่ผ่านกฎปกปิดข้อมูลแล้ว; ไม่ส่ง Facebook และไม่เปลี่ยนกฎรับออเดอร์จริง

## สรุปหลักฐานจากประวัติ

- ข้อความจากลูกค้า 5,027 ข้อความ
- catalog ปัจจุบันมีสินค้า 15 รายการ และ alias ที่ผู้ดูแลอนุมัติ 1 รายการ (`ไก่ผสม`)
- สัญญาณในข้อความลูกค้า (หนึ่งข้อความอาจอยู่หลายกลุ่ม): เริ่มสั่ง 2,155, ส่ง/เดลิเวอรี่ 877,
  ชำระเงิน 401, เปิดร้าน 175, แก้ไข/ยกเลิก 135, ถามราคา 125, คิว/เวลารอ 114,
  ถามวัตถุดิบหรือของหมด 84
- 581 จาก 608 บทสนทนามีสัญญาณออเดอร์; 433 มีทั้งออเดอร์และรายละเอียดส่ง;
  241 มีทั้งออเดอร์และชำระเงิน; 177 มีคำถามที่ต้องให้แอดมิน/สถานะจริงตอบ
- รูปแบบที่พบมากคือชื่อสินค้าติดกันโดยไม่เว้นวรรคตามด้วยเลขอารบิกและคำลงท้าย
  เช่นโครงสร้าง `สินค้า+สินค้า+จำนวน+คำลงท้าย` และ `เอา+สินค้า+จำนวน`
- ข้อความเกี่ยวกับ `กล่อง`, `ห่อ`, `ธรรมดา`, `พิเศษ`, `เพิ่ม`, `ไม่เอา`, `ไม่ใส่`
  พบจริง จึงควรแยกเป็น option/special request แทนการถือเป็นชื่อสินค้าเสมอ

## การตัดสินใจที่ผู้ดูแลยืนยันแล้ว

| เรื่อง | กฎที่อนุมัติ |
| --- | --- |
| เมนูชุด | `ข้าวหมกไก่ทอด` และ `ข้าวหมกไก่ต้ม` เป็นสินค้า 1 ชุด; `ข้าวหมกไก่ผสม` เป็นเมนูชุด 60 บาท |
| บรรจุภัณฑ์ | ค่าเริ่มต้นคือ `ห่อ`; `กล่อง` เป็นตัวเลือกบรรจุภัณฑ์ฟรี ไม่มีราคาเพิ่ม |
| รายการที่ไม่รับ | ถ้าพบสินค้าหรือตัวเลือกที่ไม่มีใน catalog หรืออยู่ในรายการไม่รับ ให้ `waiting_for_admin` ไม่สร้างออเดอร์ |
| การเรียนรู้จาก handoff | ทุก `waiting_for_admin` ต้องเก็บข้อความที่ปกปิดแล้ว, intent/candidate, เหตุผล, คำตอบหรือคำตัดสินของแอดมิน และสถานะปิดเคส เพื่อสร้างกฎต่อไป |
| การทดสอบพฤติกรรม | ใช้ข้อความจริงที่ปกปิดแล้วจากประวัติเป็น test corpus; ไม่สร้างตัวอย่างลูกค้าสมมติเพื่อใช้ตัดสินกฎ |
| คำตอบ/รับออเดอร์จากสต็อก | ตอบหรือรับรายการได้เฉพาะเมื่อ `stock_quantity` เพียงพอกับจำนวนที่ขอ; ถ้าเป็น `0` หรือไม่พอ ให้ `waiting_for_admin` |
| น้ำจิ้ม | เป็นสินค้าปกติราคา 0 บาทที่ตั้ง `สต็อกไม่จำกัด`; ไม่ตัดสต็อกเมื่อรับออเดอร์ และผู้ดูแลตั้งสถานะ `หมด` ได้จากหน้าสินค้า |

## Baseline: ผลรัน Parser v2 กับข้อความจริงที่อนุมัติแล้ว

รันในโหมดทดสอบกับข้อความลูกค้า 5,027 ข้อความจาก snapshot #2 (63 batch ที่อนุมัติแล้ว)
โดยไม่สร้าง draft order, ไม่ส่ง Facebook และไม่บันทึกข้อความประวัติซ้ำ ผลลัพธ์เป็นตัวเลขรวมเท่านั้น:

| ผล | จำนวน |
| --- | ---: |
| จับเป็น `start_order` | 2,341 |
| จับเป็นคำถามราคา/เมนู | 130 |
| คำถามที่ต้องรอแอดมิน (สต็อก/เปิดร้าน/คิว/ส่ง) | 471 |
| การชำระเงิน | 179 |
| ข้อความที่ยังไม่ครอบคลุม | 1,900 |
| ออเดอร์ที่มีสินค้าจับตรง/alias ได้ | 1,039 รายการที่ match |
| ส่งต่อแอดมินทั้งหมดใน baseline | 3,974 |

ช่องว่างหลักคือ `unmatched_product` 1,434 ข้อความ และ `unknown_message` 1,900 ข้อความ
จึงต้องนำเฉพาะ handoff ที่ผู้ดูแลตอบแล้วมาสร้าง alias/rule เพิ่ม ไม่ควรเปิด fuzzy ให้เลือกสินค้าเอง
ผลนี้เรียกดูซ้ำแบบ aggregate ได้ที่ `GET /api/parser-v2/history-summary` (owner/manager เท่านั้น)
และ endpoint นี้อ่านเฉพาะ snapshot ที่ปกปิดและอนุมัติแล้ว

## ข้อค้นพบสำคัญ

1. ชื่อตาม catalog เป็นส่วนประกอบของข้อความสั่งซื้อ ไม่ได้ปรากฏเป็นชื่อสินค้าเดี่ยวเสมอไป
   โดย `ข้าวหมกไก่ทอด` และ `ข้าวหมกไก่ต้ม` ที่พิมพ์ติดกันคือเมนูชุดเดียวตามที่ผู้ดูแลยืนยัน
2. คำสั้น เช่น `ข้าว`, `ไก่`, `หมก` มีแนวโน้มเกี่ยวข้องกับสินค้าในร้าน แต่กำกวมเกินกว่าจะ
   เพิ่มเป็น alias ได้เอง ต้องถามกลับหรือให้ผู้ดูแลอนุมัติเป็นรายคำ
3. ข้อความเปิดร้าน, ของหมด, คิว และเวลารอมีปริมาณมากและต้องพึ่งสถานะปัจจุบัน จึงเป็น
   `waiting_for_admin` จนกว่าจะมีแหล่งสถานะที่ยืนยันได้
4. มีข้อมูล delivery/payment มากพอสำหรับสร้าง state machine แต่ไม่ใช่หลักฐานว่า
   ระบบตอบเรื่องค่าส่งหรือวิธีชำระเงินได้เอง จนกว่าจะตั้งค่าข้อมูลร้านที่ยืนยันแล้ว

## 1. Draft: Text normalization rules

| ลำดับ | กฎ | ผลลัพธ์ | สถานะ |
| --- | --- | --- | --- |
| N1 | แปลงเลขไทย `๐-๙` เป็น `0-9` | จำนวนใช้รูปแบบเดียวกัน | draft |
| N2 | ลดช่องว่างซ้ำ, line break และเครื่องหมายซ้ำ | การ match สม่ำเสมอ | draft |
| N3 | แทรกขอบเขตระหว่างเลขกับตัวอักษร | `สินค้า1ค่ะ` → `สินค้า 1 ค่ะ` | draft |
| N4 | แปลง `x2`/`2x` เป็นรูปจำนวนมาตรฐาน | เก็บ `quantity=2` | draft |
| N5 | ใช้ `+`, `และ`, newline เป็นตัวคั่นรายการ | รองรับหลายรายการในข้อความเดียว | draft |
| N6 | แยกคำลงท้ายท้ายข้อความ (`ค่ะ`, `ครับ`, `คับ`, `ค่า`) ออกจากข้อความคำสั่ง | ไม่กระทบชื่อสินค้า | draft |
| N7 | ทำคำย่อหรือคำสะกดให้เป็นมาตรฐานเฉพาะเมื่ออยู่ใน alias ที่อนุมัติแล้ว | ป้องกันการเดา | draft |
| N8 | คงข้อความดิบที่ผ่านการปกปิดไว้ควบคู่กับ normalized text | ตรวจสอบย้อนกลับได้ | draft |

## 2. Draft: Dictionary และ alias matching

### กฎที่เสนอ

- จับชื่อสินค้าแบบ exact match ก่อน แล้วจึงจับ alias ที่อนุมัติ
- scan ชื่อที่ยาวที่สุดก่อน และห้ามให้คำที่สั้นกว่า match ซ้อนในชื่อที่ยาวกว่า
- หลังแยกชื่อสินค้าแล้ว ให้ตีความจำนวน/หน่วย/ตัวเลือกในช่วงใกล้เคียงของสินค้านั้น

### ข้อเสนอ alias / phrase ที่ต้องตรวจทาน

| ข้อเสนอ | การตีความที่เป็นไปได้ | ข้อควรทำ |
| --- | --- | --- |
| `ข้าวหมกไก่ทอด` ติดกัน | เมนูชุดเดียว | match สินค้า `ข้าวหมกไก่ทอด` (45 บาท) |
| `ข้าวหมกไก่ต้ม` ติดกัน | เมนูชุดเดียว | match สินค้า `ข้าวหมกไก่ต้ม` (45 บาท) |
| `หมก` | อาจหมายถึง `ข้าวหมก` | `needs_review`; ถามยืนยันหากไม่มีสินค้าอื่นชัดเจน |
| `ข้าว` | อาจหมายถึง `ข้าวหมก` | ห้าม auto-match เพราะอาจไม่ชัดเจน |
| `ไก่` | อาจหมายถึงไก่ทอดหรือไก่ต้ม | ห้าม auto-match; ต้องถามเลือก |
| `น่องไก่ทอด`, `น่องไก่ต้ม`, `ปีกไก่ทอด`, `ปีกไก่ต้ม` | สินค้าเพิ่ม | เพิ่ม catalog แล้ว ราคา 15 บาทต่อรายการ |
| `ไก่ผสม` | เมนูชุด | match สินค้า `ข้าวหมกไก่ผสม` (60 บาท) |
| เครื่องดื่ม/ของเพิ่มที่ไม่อยู่ catalog | ร้านยังไม่รับรายการนี้ผ่านระบบ | `waiting_for_admin`; เก็บ feedback เพื่อพิจารณาเพิ่มสินค้า |

## 3. Draft: Regex extraction และ intent rules

ให้ใช้ลำดับความสำคัญต่อไปนี้เมื่อข้อความเข้าหลายกฎ:

1. ทุกคำถาม รวมถึงสถานะจริง: ของหมด, เปิดร้าน, คิว, เวลารอ, ร้องเรียน, ต่อรองราคา
2. ถามราคา/เมนู (ตอบได้เมื่อ catalog และราคาที่ตั้งค่าไว้ยืนยันแล้ว)
3. ยกเลิก/ลด/ไม่เอา/ไม่ใส่ที่ผูกกับรายการที่มีอยู่
4. รายละเอียดส่งและยืนยันการชำระเงิน (เติม state ไม่ตอบเป็นข้อเท็จจริงใหม่)
5. เริ่ม/เพิ่มออเดอร์ เฉพาะหลังไม่มีคำถามค้างอยู่
6. ทักทายหรือข้อความไม่ชัดเจน

คำถามต้องชนะการตรวจพบชื่อสินค้าเสมอ ตัวอย่างเช่นข้อความที่พบในลักษณะ
`ไก่ทอดหมดยังคะ` ต้องเป็น `ask_availability` ไม่ใช่คำสั่งซื้อ

### ประตูกันคำถาม (ปรับตามการตัดสินใจของผู้ดูแล)

| ประเภท | คำ/รูปแบบ | การทำงาน |
| --- | --- | --- |
| ตอบได้จากข้อมูลที่ยืนยัน | `ราคา`, `กี่บาท`, `เท่าไหร่`, `เท่าไร` + พบสินค้าใน catalog | `ask_price` → `answer_if_verified` |
| ตอบได้จาก catalog | `เมนู`, `มีอะไรบ้าง` | `ask_menu` → `answer_if_verified` |
| ตอบจาก stock ที่ยืนยัน | `มีน้ำอะไรบ้าง`, `มีเมนูอะไรบ้าง`, `ขอไก่เพิ่มได้ไหม`, `เลือกส่วนไก่ได้ไหม` | แสดงเฉพาะสินค้าที่ `stock_quantity > 0`; ถ้าไม่มี ให้ส่งแอดมิน |
| ตอบจากสินค้าสต็อกไม่จำกัด | `ขอน้ำจิ้มเพิ่มได้ไหม` | ตอบได้เมื่อสถานะน้ำจิ้มเป็น `มี`; ไม่ลดสต็อกอัตโนมัติ |
| ประตูกันคำถามทั่วไป | `ไหม`, `มั้ย`, `มั๊ย`, `มัย`, `หรือ`, `รึ`, `เหรอ`, `หรอ`, `อะไร`, `กี่`, `ที่ไหน`, `เมื่อไหร่`, `ยังไง`, `ไหน`, `ปะ`, `เปล่า`, `ป่าว`, `?` | `ask_admin` → `waiting_for_admin`; ไม่สร้างออเดอร์และไม่ต้องแยกประเภทย่อย |

คำถามทั่วไปยังบันทึกเป็น handoff ที่ปกปิดแล้วตามกฎเดิม เพื่อให้แอดมินตอบและตรวจสอบย้อนหลังได้
แต่จะไม่สร้างคลัง intent ย่อย เช่น `ask_opening_hours` หรือ `ask_delivery` เพิ่มอีก

| Intent | สัญญาณ draft | Slot ที่ต้องดึง | การทำงาน |
| --- | --- | --- | --- |
| `start_order` | `เอา|รับ|สั่ง|ขอ` หรือพบสินค้า+จำนวน | product, quantity, unit, option | เปิด/เติมตะกร้าร่าง |
| `add_item` | เพิ่มสินค้าเมื่อมีตะกร้าร่าง | product, quantity | เพิ่มเฉพาะรายการที่ชัดเจน |
| `edit_item` | เพิ่ม/ลด/เปลี่ยน/ธรรมดา/พิเศษ | target item, delta, option | ถามกลับเมื่อไม่รู้ว่าแก้รายการใด |
| `cancel_item` | ไม่เอา/ยกเลิก | target item | ยืนยันเป้าหมายก่อนลบหากมีหลายรายการ |
| `ask_price` | ราคา/กี่บาท/เท่าไหร่ | product | ตอบเฉพาะราคาจาก catalog ที่ยืนยัน |
| `ask_availability` | มีไหม/หมดไหม/หมดยัง | product | `waiting_for_admin` หากไม่มี stock status ยืนยัน |
| `ask_opening_hours` | เปิดไหม/เปิดมั้ย | none | `waiting_for_admin` |
| `ask_queue_eta` | คิว/กี่นาที/รอนาน | none | `waiting_for_admin` |
| `delivery_details` | ส่ง/ที่เดิม/วางหน้า | delivery context | ขอจุดส่งที่ยังขาด |
| `payment` | โอน/สลิป/จ่าย/สิทธิ์ชำระ | payment state | ตรวจตามวิธีชำระที่ตั้งค่าไว้หรือส่งแอดมิน |

## 4. Draft: Fuzzy matching policy

- ใช้หลัง exact และ approved alias เท่านั้น
- แสดง candidate สูงสุดและอันดับสอง พร้อมคะแนน แต่ **ไม่เพิ่มสินค้าเข้าตะกร้าเอง**
- ค่าเริ่มต้นเพื่อทดลองใน test environment: similarity >= 92/100 และต่างจากอันดับสอง >= 8 คะแนน
- กรณี `หมก`, `ข้าว`, `ไก่` แม้คะแนนสูงก็ยังเป็น `needs_confirmation` เพราะเป็นคำสั้น/กำกวม
- ทุก candidate ที่ผู้ดูแลยอมรับหรือปฏิเสธเก็บใน `fuzzy_match_reviews` เพื่อนำไปปรับ threshold
  หรือเปลี่ยนเป็น alias ที่อนุมัติภายหลัง

## 5. Draft: Intent training และ keyword weighting

เริ่มจาก rules/weights ที่ตรวจสอบได้ ไม่สร้าง Naive Bayes ในรอบนี้ เพราะยังไม่มีชุดข้อความ
ที่ผู้ดูแลติดป้าย intent อย่างเป็นทางการ

### Label set สำหรับเก็บตัวอย่าง

`greeting`, `start_order`, `add_item`, `edit_item`, `cancel_item`, `ask_price`,
`ask_menu`, `ask_availability`, `ask_opening_hours`, `ask_queue_eta`,
`delivery_details`, `payment`, `complaint`, `handoff`, `unknown`

### Keyword weights เริ่มต้น

| Intent | คีย์เวิร์ดบวก | คีย์เวิร์ดที่ต้องลดความมั่นใจ |
| --- | --- | --- |
| `ask_price` | ราคา, กี่บาท, เท่าไหร่ | พบหลายสินค้าแต่ไม่ระบุว่าจะถามรายการใด |
| `delivery_details` | ส่ง, ที่เดิม, วางหน้า | ไม่มีจุดส่ง/บริบทส่ง |
| `payment` | โอน, สลิป, จ่าย | ไม่ทราบว่าผูกกับออเดอร์ใด |
| `handoff` | เปิดไหม, หมดไหม, คิว, กี่นาที, ร้องเรียน | ทุกกรณีนี้ต้องรอสถานะหรือคนตอบ |
| `start_order` | เอา, รับ, สั่ง, ขอ + สินค้าชัดเจน | คำกริยาอย่างเดียวโดยไม่พบสินค้า |

## 6. Draft: State machine

หลักฐานจากประวัติพบลำดับ `order → delivery` บ่อยที่สุด (324 บทสนทนา) และพบ
`order → payment` (51 บทสนทนา); มีคำถามสถานะมาก่อนออเดอร์ 61 บทสนทนา

```text
idle
 ├─ สินค้าชัดเจน → collecting_items
 ├─ คำถามราคา/เมนู → answer_if_verified | waiting_for_admin
 ├─ ถามเปิดร้าน/ของหมด/คิว/เวลารอ/ร้องเรียน → waiting_for_admin
 └─ ไม่ชัดเจน → clarify_intent

collecting_items
 ├─ เพิ่ม/แก้/ยกเลิก → collecting_items
 ├─ ข้อมูลสินค้า/จำนวน/ตัวเลือกครบ → collecting_delivery_details
 └─ กำกวม → clarify_item

collecting_delivery_details
 ├─ จุดส่งพอใช้ได้ → collecting_payment_or_confirmation
 └─ ไม่ครบ → ask_delivery_detail

collecting_payment_or_confirmation
 ├─ เงื่อนไขที่ยืนยันแล้วครบ → ready_for_human_confirmation
 └─ ข้อมูลไม่พอ/ข้อขัดแย้ง → waiting_for_admin
```

ระบบต้องไม่ยืนยันออเดอร์เองใน Phase 2 และต้องไม่สร้างคำตอบเกี่ยวกับสต็อก, เวลาเปิด,
คิว หรือเวลาส่งโดยไม่มีแหล่งข้อมูลยืนยัน

## Test corpus จากข้อความจริงก่อนใช้งานจริง

สร้าง corpus จากข้อความจริงที่ผ่านการปกปิดและผู้ดูแลติดป้ายแล้วเท่านั้น โดยแยกเป็น
ชุดสร้างกฎและชุดทดสอบที่กันไว้คนละบทสนทนา เพื่อไม่ให้ข้อความเดียวกันอยู่ทั้งสองชุด

ต้องครอบคลุมรูปแบบที่พบจริง: ชื่อสินค้า+จำนวนติดกัน, หลายรายการคั่นด้วย `+`/`และ`/newline,
จำนวนหน้า/หลังสินค้า, ปกติ/พิเศษ/ห่อ/กล่อง, เพิ่ม/ไม่เอา/ไม่ใส่, คำพิมพ์สั้น/ผิด,
คำถามราคา/สต็อก/เปิดร้าน/คิว/เวลารอ/ต่อรอง/ร้องเรียน, จุดส่งไม่ครบ, โอน/สลิป,
หลาย intent ในข้อความเดียว และการปกปิด PII

## ข้อเสนอสำหรับการอนุมัติรอบถัดไป

1. เพิ่มรส/ภาชนะของน้ำอัดลมเป็นสินค้าแยก เมื่อมีชื่อและราคาเฉพาะ
2. อนุมัติ label set, intent priority, state machine และ handoff log schema ก่อนเขียน parser v2
