# Order parser v2 — กฎเพิ่มเติม (เตรียมไว้ทำทีหลัง)

สถานะ: **แผน/สเปกสำหรับงานถัดไป ยังไม่ได้ implement** ต่อยอดจาก `order_parser.py` (v1)
ที่ทำไปแล้วใน Phase 1 (ดู README.md หัวข้อ "ข้อจำกัดของการตีความคำสั่งซื้อ" สำหรับ
ข้อจำกัดของ v1 ที่เอกสารนี้พยายามแก้)

ทุกข้อด้านล่างมาจากโจทย์ที่ผู้ใช้ให้มา พร้อมการวิเคราะห์ความเป็นไปได้/แนวทาง/
สิ่งที่ต้องตัดสินใจเพิ่มก่อนเริ่มเขียนโค้ดจริง

---

## 1. ขยาย rule negation ให้ตรวจคำปฏิเสธที่อยู่ "หลัง" คำด้วย

ตอนนี้ (v1) เช็คเฉพาะคำปฏิเสธที่อยู่ **หน้า** คำที่ match (เช่น "ไม่ใส่ถั่ว") แต่ภาษาไทย
พูดปฏิเสธ "ต่อท้าย" ก็บ่อยพอกัน เช่น "ผัดไทยกุ้ง ไม่เอา", "ถั่ว ไม่ต้องนะ"

**แนวทาง:** เพิ่มการเช็คข้อความ "หลัง" คำที่ match ด้วย (ใช้ negation window เดียวกัน
หรือแยก window) แล้ว `negated = negated_before OR negated_after`

**ต้องตัดสินใจ:** ถ้าคำเดียวกันอยู่ระหว่างสองคำที่ match ติดกัน เช่น
"ผัดไทยกุ้ง ไม่เอา ส้มตำ" — คำว่า "ไม่เอา" ควรนับเป็น negation ของ "ผัดไทยกุ้ง" (คำก่อนหน้า)
เท่านั้น ไม่ใช่ของ "ส้มตำ" (คำถัดไป) ด้วย — ต้องกำหนดกฎ priority ให้ชัด (เช่น คำปฏิเสธ
ที่อยู่ตรงกลางระหว่าง 2 match จะผูกกับ match ทางซ้ายเสมอ ไม่ผูกกับขวา)

## 2. ทำระยะเช็คคำปฏิเสธ (negation window) ให้ปรับขนาดได้

ตอนนี้ hardcode ไว้ที่ 8 ตัวอักษร (`NEGATION_WINDOW_CHARS`) ในโค้ด

**แนวทาง:** ย้ายค่านี้ไปเป็น setting ที่ปรับได้ (เช่น เพิ่มใน `ShopSettings` หรือเป็น env var)
แทนการ hardcode เผื่ออนาคตพบว่าต้องขยาย/หดตามการใช้งานจริง โดยไม่ต้องแก้โค้ด

**ทางเลือก:** จะนับเป็น "จำนวนตัวอักษร" เหมือนเดิม หรือเปลี่ยนเป็น "นับเป็นคำ" (เช่น
ย้อนไป 2 คำก่อนหน้า แบ่งด้วยช่องว่าง) ซึ่งทนทานต่อความยาวคำมากกว่า — ภาษาไทยไม่มี
เว้นวรรคระหว่างคำในประโยคปกติ จึงนับ "คำ" ยากกว่าภาษาอังกฤษ ต้องตัดสินใจว่าจะใช้
ตัวตัดคำ (word segmentation library เช่น `pythainlp`) หรือยังคงนับเป็นตัวอักษรต่อไป

## 3. รองรับตัวเลขและคำที่หมายถึงตัวเลข (หนึ่ง, สอง, ...)

ตอนนี้ v1 ไม่ตีความจำนวนเลย (ทุก match = quantity 1 เสมอ)

**แนวทาง:**
- รองรับเลขอารบิก ("2 จาน", "x2") ด้วย regex จับตัวเลขที่อยู่ติดกับคำที่ match
- รองรับเลขไทยที่เขียนเป็นคำ: หนึ่ง/นึง, สอง, สาม, สี่, ห้า, หก, เจ็ด, แปด, เก้า, สิบ
  (รวมคำเชื่อม เช่น "สิบเอ็ด", "ยี่สิบ") — ทำเป็น lookup table คำ → ตัวเลข
- ต้องตัดสินใจตำแหน่งของเลข: อยู่ "หน้า" เมนู ("ขอ 2 ส้มตำ") หรือ "หลัง" เมนู
  ("ส้มตำ 2 จาน") หรือทั้งคู่ — แนะนำรองรับทั้งคู่เหมือน negation
- หน่วยนับ (จาน/ที่/แก้ว/กล่อง) ไม่จำเป็นต้องตีความความหมาย แค่ใช้เป็นสัญญาณว่า
  เลขที่อยู่ติดกันคือ "จำนวน" ไม่ใช่ส่วนหนึ่งของชื่อเมนู (กันกรณีชื่อเมนูมีเลขอยู่ในชื่อ)

## 4. อัปเกรดอัลกอริทึมการจับคู่คำ (matching)

### 4.1 ค้นหา 2 แบบ: exact match และ full-coverage match
- **แบบที่ 1 (มีอยู่แล้วใน v1):** substring exact match — คำในข้อความตรงกับชื่อสินค้า/
  ท็อปปิ้ง/วัตถุดิบแบบเป๊ะๆ
- **แบบที่ 2 (ใหม่):** ตรวจว่า **ข้อความทั้งหมด** ที่ลูกค้าพิมพ์มา (ทุกคำ ไม่เหลือคำที่
  ตีความไม่ได้) ประกอบกันแล้วตรงกับเมนูใดเมนูหนึ่งในระบบ — เช่น ลูกค้าพิมพ์คำเรียงแบบ
  สลับลำดับ หรือมีคำเชื่อมแทรกที่ชื่อในระบบไม่มี แต่เนื้อหาคำสั่งครบทุกคำ

  **ต้องตัดสินใจ:** เกณฑ์ "ครบทุกคำ" หมายถึงอะไรแน่ชัด — ทุก token ในข้อความต้อง
  ปรากฏในชื่อเมนู (อนุญาตให้สลับลำดับ) ใช่ไหม หรืออนุญาตให้มีคำฟุ่มเฟือย (เช่น "ขอ",
  "หน่อย", "ค่ะ") ปนอยู่ได้โดยไม่นับเป็นคำที่ต้องครบ — แนะนำกำหนด stop-word list
  (ขอ, หน่อย, ค่ะ, ครับ, ด้วย ฯลฯ) แล้วตัดออกก่อนเช็คความครบถ้วน

### 4.2 Fallback เป็น fuzzy matching ทีละตัวอักษร (กันพิมพ์ผิด)
ถ้า 4.1 ทั้งสองแบบหาไม่เจอ ให้ใช้ fuzzy string matching เทียบกับชื่อในระบบ โดยดู 2 ค่า:
- **% ลำดับเหมือนกัน** (sequence similarity) — เหมาะกับ `difflib.SequenceMatcher.ratio()`
  ของ Python (built-in ไม่ต้องลงไลบรารีเพิ่ม) ซึ่งคำนวณจาก longest matching blocks
- **% ตัวอักษรเหมือนกัน** (character overlap) — เช่นสัดส่วนตัวอักษรร่วมกัน
  (อาจใช้ Levenshtein/edit distance แทนหรือเสริม — ต้องเพิ่ม dependency เช่น
  `python-Levenshtein` หรือเขียน DP เอง)

**ต้องตัดสินใจ:** เกณฑ์ threshold เท่าไหร่ถึงจะถือว่า "match" (เช่น >= 80%?) และถ้ามี
หลายชื่อที่ similarity ใกล้เคียงกันจะเลือกตัวไหน (เลือกตัวสูงสุด + ต้องห่างจากอันดับ 2
พอสมควรเพื่อกันความกำกวม) — แนะนำว่าผลจาก fuzzy match ควร **flag ไว้ให้พนักงานเห็น
ชัดเจนว่าเป็นการเดา** (ไม่ใช่ match ตรงเป๊ะ) เพราะความเสี่ยงผิดสูงกว่า exact match มาก

### 4.3 ตรวจจับคำย่อ (เช่น "กพ" = "กพ." = "ก.พ." = "กระเพรา")
คำย่อแบบนี้เดาจากตัวอักษรไม่ได้ (ไม่ใช่ fuzzy matching ปกติ) ต้องเป็น **mapping ตายตัว**
ที่ผูกกับสินค้าแต่ละตัวในระบบ

**แนวทาง:** เพิ่มตาราง `ProductAlias` (product_id, alias_text) ให้พนักงาน/เจ้าของร้าน
กำหนดคำย่อเองได้ผ่านหน้า backoffice (เช่นหน้าแก้ไขสินค้า เพิ่มช่อง "คำย่อ/ชื่อเล่น"
กรอกได้หลายคำ คั่นด้วยจุลภาค) แล้วให้ parser เช็ค alias เป็นอีกหนึ่ง catalog term
เหมือน modifier/ingredient — วิธีนี้ scale ได้กับทุกร้าน ไม่ต้อง hardcode คำย่อภาษาไทย
ในโค้ด (เพราะคำย่อแต่ละร้านไม่เหมือนกัน)

## 5. เก็บ list คำที่พิมพ์มาแล้วไม่ตรงกับเมนูไหนเลย

**แนวทาง:** เพิ่มตาราง `UnmatchedTermLog` (หรือฟิลด์ในข้อความ) บันทึกคำ/วลีที่ parser
ไม่สามารถจับคู่กับเมนู/ท็อปปิ้ง/วัตถุดิบใดได้เลย (ทั้ง exact, full-coverage, fuzzy) — ใช้
ประโยชน์ 2 ทาง:
- หน้า backoffice ใหม่ "คำที่ไม่รู้จัก" ให้เจ้าของร้านดูว่าลูกค้าพิมพ์อะไรบ่อยที่ระบบไม่เข้าใจ
  แล้วไปเพิ่มเป็นคำย่อ/ชื่อสินค้าใหม่ได้ (ปิด loop กับข้อ 4.3)
- เป็นข้อมูลเบื้องต้นถ้าวันหนึ่งจะอัปเกรดเป็น NLU/LLM จริงจัง

## 6. ตรวจจับท็อปปิ้งที่ใช้กับ "สองเมนู" พร้อมกัน (ใส่ทั้งคู่ / ใส่แค่อันเดียว)

โจทย์: ปกติท็อปปิ้งจะพิมพ์ตามหลังชื่อเมนู เช่น "ผัดไทยกุ้ง ใส่ถั่ว" — แต่ถ้ามี 2 เมนูอยู่
ก่อนหน้าท็อปปิ้งคำเดียว เช่น "ผัดไทยกุ้ง กับ ผัดไทยหมู ใส่ถั่ว" ต้องตรวจคำเชื่อมระหว่าง
สองเมนูว่าหมายถึง **ใส่ทั้งคู่** (เช่น "กับ", "และ", "ทั้งคู่", "ทั้งสองอย่าง") หรือ
**ใส่แค่อันใดอันหนึ่ง** (เช่น "หรือ", "เลือก")

**แนวทาง:** เพิ่มตาราง/list คำเชื่อม 2 ประเภท เก็บใน DB (เหมือนแนวทางคำย่อในข้อ 4.3
คือให้แก้ไขได้ผ่าน backoffice ไม่ hardcode):
- `ConjunctionWord` ประเภท "both" (กับ, และ, ทั้งคู่, ทั้งสองอย่าง)
- `ConjunctionWord` ประเภท "either" (หรือ, เลือก, แล้วแต่)

แล้วปรับ logic ตอนเจอ modifier match: ถ้าเจอ 2 product match ติดกันก่อนหน้า modifier
(คั่นด้วยคำใน list ใดลิสต์หนึ่ง) → ใช้ "both" = แนบ special_request ให้ทั้ง 2 รายการ,
"either" = ต้องถามพนักงาน/ทำเครื่องหมายให้ชัดว่าระบบไม่แน่ใจว่าใส่อันไหน (อย่า auto
เลือกเอง เพราะเดาผิดมีผลเสียมากกว่าปล่อยให้พนักงานเช็ค)

---

## ภาพรวมการเปลี่ยนแปลง schema ที่ต้องเพิ่ม (สรุปจากทุกข้อ)

| ตาราง/ฟิลด์ใหม่ | ใช้กับข้อ | หมายเหตุ |
|---|---|---|
| `ProductAlias` (product_id, alias_text) | 4.3 | คำย่อ/ชื่อเล่นต่อสินค้า แก้ไขได้จาก backoffice |
| `ConjunctionWord` (text, kind: both/either) | 6 | คำเชื่อมระหว่าง 2 เมนู แก้ไขได้จาก backoffice |
| `UnmatchedTermLog` (text, conversation_id, created_at) | 5 | log คำที่ไม่ match อะไรเลย |
| negation window เป็น setting แทน constant | 2 | ปรับได้โดยไม่ต้องแก้โค้ด |
| Thai number-word lookup (`หนึ่ง`→1 ฯลฯ) | 3 | เป็น constant ในโค้ดได้ (ไม่ผันตามร้าน) |

## ลำดับความเสี่ยง/ความซับซ้อนถ้าจะ implement จริง (มากไปน้อย)

1. **ข้อ 4 (matching v2 ทั้ง 3 ข้อย่อย)** — ซับซ้อนสุด โดยเฉพาะ 4.1 full-coverage และ
   4.2 fuzzy matching เพราะมีความเสี่ยง false positive สูง (จับคู่ผิดเมนู) ต้องคิดเรื่อง
   threshold และการแสดงผลให้พนักงานเห็นว่า "นี่คือการเดา ไม่ใช่ match ชัวร์" ให้ดี
2. **ข้อ 6** — ต้องอาศัยผลจากข้อ 4 ที่แม่นยำพอสมควรก่อน (เพราะต้องตรวจจับ "2 เมนูติดกัน"
   ให้ถูกต้องก่อน ถึงจะไปเช็คคำเชื่อมต่อได้)
3. **ข้อ 1, 2, 3** — ทำได้ตรงไปตรงมา ความเสี่ยงต่ำ ขยายจากโครงสร้าง v1 เดิมโดยตรง
4. **ข้อ 5** — ง่ายสุด เป็นแค่ logging ไม่กระทบ logic การ match เดิม ทำเป็นอันดับแรกได้
   เพื่อเริ่มเก็บข้อมูลไว้ใช้ตัดสินใจเรื่อง 4.3/6 ในอนาคต

**ข้อเสนอลำดับการทำ (ถ้าจะเริ่ม):** 5 → 1,2,3 → 4.3 (alias) → 4.1 → 4.2 → 6
(เรียงจากเสี่ยงน้อย/ได้ผลเร็ว ไปหาเสี่ยงเยอะ/ซับซ้อน)
